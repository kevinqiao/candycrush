import { CandyModel } from "../../model/CandyModel";
import { CellItem } from "../../model/CellItem";

const useCollectCandies = (scene: any) => {

    const playCollect = (cells: CellItem[], candyMap: Map<Number, CandyModel>, tl: any) => {

        cells.forEach((c, index) => {

        })
    }
    return { playCollect }


}
export default useCollectCandies